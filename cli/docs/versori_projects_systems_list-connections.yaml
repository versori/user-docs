name: versori projects systems list-connections
synopsis: List static connections for a project environment
usage: versori projects systems list-connections --project <project-id> --environment <environment-name> [flags]
options:
    - name: environment
      usage: The environment name within the project
    - name: help
      shorthand: h
      default_value: "false"
      usage: help for list-connections
    - name: project
      usage: The project ID to list connections for
inherited_options:
    - name: config
      shorthand: c
      default_value: /Users/teodor/.versori/config.yaml
      usage: Config file (default is $HOME/.versori/config.yaml)
    - name: context
      shorthand: x
      usage: |
        Load a specific context. If not set will use the default active one.
    - name: output
      shorthand: o
      default_value: table
      usage: Output format (yaml, json, table)
see_also:
    - versori projects systems - Manage systems within a project
