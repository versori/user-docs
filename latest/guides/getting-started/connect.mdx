---
title: Managing Connectors
---

This guide explores the Connect tab and explains how Connectors and Connections can be managed. To understand more about
how Connectors and Connections work within the Versori Platform, see the [Connectors](/latest/concepts/connectors) and
[Connections](/latest/concepts/connections) pages.

<Note>
    This guide does not dive into specific authentication methods, see [Authentication
    Methods](/latest/guides/auth-methods/introduction) for more details on the different methods available.
</Note>

The connect tab shows the user the Connectors which are in use by the Project, including the list of endpoints available
to each Connector. Here you can also manage authentication by configuring different authentication methods for each
Connector.

<Frame className="max-w-2xl mx-auto">
    <img src="/images/guides/connect/connectors-001.png" />
</Frame>

## Connectors

Connectors are the starting point in how Versori provides integrations to external systems. Unlike other iPaaS's,
Versori do not rely on “pre-built” connectors, we use AI to generate Connectors either by scraping API documentation or
from a user-provided OpenAPI specification.

### Managing Connectors

Connectors can be created 2 ways, through AI chat or manually via the Connector sidebar.

<Note>
    If Connectors are created through Plan then the associated endpoints the Plan agent has detected as necessary for
    the integration will be a part of the Connector and will be accseible, in the Connect tab, to test.
</Note>

When selecting a Connector, it‘s overview page will present information on its active connection, or in the case of an
Embedded Connector, its configuration, the connector‘s base url, and any existing connections for that connector.

<Frame className="max-w-2xl mx-auto">
    <img src="/images/guides/connect/connector-overview.png" />
</Frame>

## Connecting

To create a connection, click 'Connect'. In order to connect an authorisation method must fist be selected matching the
authentication method needed by the connector, or should a new authentication method be required, one can be newly
created. The corresponding credential form will then display allowing the connection to be finalised.

<Frame className="max-w-2xl mx-auto">
    <img src="/images/guides/connect/connector-authorisation.png" />
</Frame>

## Embedded Connector

An embedded connector enables multi tenanted integrations that external user‘s of an integration can use their own
credentials to connect to a connector with.

To enable an embedded connector, the authentication method required must be selected.

<Columns cols={2}>
    <Card title="Build Workflows">
        <img src="/images/guides/connect/embedded-connector.png" />
    </Card>
    <Card title="Workflow Information">
        <img src="/images/guides/connect/embedded-auth-selection.png" />
    </Card>
</Columns>

#### Bypass Auth

In order to bypass authentication for a given connector; an authentication of type 'None' should be selected, and then
the connection can be finalised by clicking 'Bypass Authentication'

<Frame className="max-w-2xl mx-auto">
    <img src="/images/guides/connect/bypass-authentication.png" />
</Frame>

## Connections

Connections are the way to connect to an external system. Each Connector can have multiple Connections, and each
Connection can have a different set of credentials and variables.
