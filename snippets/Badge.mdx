export const Badge = ({ children, color = 'red', size = 'md' }) => (
    <span
        className={`px-2 py-0.5 rounded-md bg-${color}-100/50 dark:bg-${color}-400/10 text-${color}-600 dark:text-${color}-300 text-${size}`}
    >
        {children}
    </span>
);
